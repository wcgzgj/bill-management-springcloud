server:
  port: 10086

spring:
  application:
    name: eureka-server

#eureka:
#  client:
#    service-url:
#      # eureka 服务地址，eureka 要写上自己的地址，如果是集群的话;需要指定其它集群eureka地址
#      defaultZone: HTTP://127.0.0.1:10086/eureka
#      # 不注册自己(这个配置是默认开启的，所以我们需要手动配置关闭)
#      register-with-eureka: false
#      # 不拉取服务(这个配置是默认开启的，所以我们需要手动配置关闭)
#      fetch-registry: false
#
eureka:
  client:
    service-url:
      defaultZone: HTTP://127.0.0.1:10086/eureka
      #不注册自己
      register-with-eureka: false
      # 不拉取服务
      fetch-register: false

